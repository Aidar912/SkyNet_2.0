import 'package:flutter/material.dart';

class SignedInPage extends StatelessWidget {
  final String userLogin;
  final bool wantsTouchId;

  const SignedInPage({Key? key,required this.userLogin , required this.wantsTouchId}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
